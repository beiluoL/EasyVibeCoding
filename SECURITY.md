# 安全策略（Security Policy）

## 报告漏洞

如果你发现安全漏洞，请**不要**在公开 issue 中提交，而是邮件至 **security@example.com**（TBD，正式安全邮箱待定）。

请在邮件中说明：

- 受影响的内容（文件 / 路径 / Skill 脚本）
- 复现步骤
- 影响范围
- 你的建议修复方向（可选）

我们会在合理时间内回复并协调修复。

> ⚠️ Note：当前 `security@example.com` 为占位符（TBD），正式邮箱将在 V0.2 前确定。

---

## 禁止提交的内容

**以下内容严禁进入本仓库**（无论以代码、注释、示例还是文档形式）：

- API keys（如 `sk-...`、`AKIA...`）
- 密码 / 口令 / 密码短语
- Access tokens / refresh tokens / bearer tokens
- 私人凭据（私钥、`.pem`、`.p12`、SSH 私钥）
- 生产环境密钥与连接串（数据库 URL、生产服务账号）
- 任何他人的个人隐私信息（邮箱、电话、地址）

提交前请自查：`git diff` 里是否混入了 `.env` / 密钥串。若已误提交，请立即**吊销该凭据**并联系维护者清理历史，而不是仅删除当前文件。

---

## Skill 脚本安全披露要求

任何带脚本 / 可执行内容的 Skill，必须在 `SKILL.md` 中**显式**文档化以下五项：

| 项 | 说明 |
| --- | --- |
| **What it does** | 这个脚本做什么 |
| **Inputs** | 接收什么输入（参数 / 文件 / 环境变量） |
| **Outputs** | 产生什么输出（文件 / 退出码 / 副作用产物） |
| **Dependencies** | 依赖什么（第三方库 / 外部命令 / 网络） |
| **Side Effects** | 副作用：是否会写文件、改配置、发网络请求、删除数据 |

缺少以上任一项的脚本 Skill 不会被合并。

---

## 禁止的脚本行为

Skill 脚本**严禁**包含以下行为：

- 🔑 **凭据窃取**：读取并外传 `~/.ssh`、`~/.aws`、浏览器凭据、密钥环等。
- 🌐 **隐藏的网络请求**：未在 Side Effects 中声明的出站请求（尤其是数据回传）。
- 🦠 **恶意软件**：投放后门、加密货币挖矿、勒索逻辑、持久化驻留。
- 🔓 **未授权访问**：尝试访问他人账号、越权调用 API、绕过认证。
- 💥 **破坏性命令**：未声明且未确认的 `rm -rf`、格式化、批量删除、覆盖系统文件。

任何被发现含上述行为的 Skill 将被立即移除，提交者将被封禁。

---

## 建议

- 所有需要凭据的示例一律使用**占位符**（如 `YOUR_API_KEY`），并注明“请勿提交真实密钥”。
- Skill 脚本默认**不联网**；确需联网时，必须在 Inputs / Side Effects 中声明目标地址与用途。
- 鼓励在 CI 中加入密钥扫描（如 `gitleaks`），防止误提交。

---

相关：[CONTRIBUTING.md](CONTRIBUTING.md) · [AGENTS.md](AGENTS.md) 的 Security Rules。
